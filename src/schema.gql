# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ImageUrl {
  thumbnail: String!
  small: String!
  original: String!
}

type Product {
  id: String!
  name: String!
  brand: String
  price: Float!
  size: String!
  stock: Int!
  description: String
  ingredients: String
  origin: String
  imageUrls: [ImageUrl!]!
  categoryIds: [Int!]!
}

type PageInfo {
  startCursor: String
  endCursor: String
  hasNextPage: Boolean!
}

type PaginatedProduct {
  edges: [ProductEdge!]
  pageInfo: PageInfo
}

type ProductEdge {
  cursor: String!
  node: Product!
}

type CartItem {
  productId: String!
  product: Product
  quantity: Int!
}

type Cart {
  userId: String!
  items: [CartItem!]!
}

type User {
  id: String!
  email: String!
  displayName: String
}

type OrderItem {
  productId: String!
  name: String!
  brand: String
  size: String!
  imageUrl: String!
  unitPrice: Float!
  quantity: Int!
}

type Order {
  id: String!
  userId: String!
  items: [OrderItem!]!
  totalAmount: Float!
  status: String!
}

type Subcategory {
  id: Int!
  name: String!
  image: String!
}

type Category {
  id: Int!
  name: String!
  image: String!
  subcategories: [Subcategory!]!
}

type Metadata {
  categories: [Category!]!
}

type Query {
  currentUser: User!
  product(id: String!): Product!
  searchProductsByTerm(searchTerm: String!, first: Int, after: String): PaginatedProduct!
  searchProductsByCategory(categoryId: Int!, first: Int, after: String): PaginatedProduct!
  cart: Cart!
  order(id: String!): Order!
  ordersByUser: [Order!]!
  metadata: Metadata!
}

type Mutation {
  updateUser(input: UpdateUserInput!): User!
  addProductToCart(productId: String!): Cart!
  changeCartProductQuantity(productId: String!, quantity: Int!): Cart!
  createOrder: Order!
}

input UpdateUserInput {
  displayName: String!
}